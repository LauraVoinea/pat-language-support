# PAT Language Support

A minimal Visual Studio Code extension that adds syntax highlighting for PAT files (`.pat`).

## Features

- PAT language registration (`.pat`)
- TextMate grammar-based syntax highlighting
- Bracket matching and auto-closing pairs
